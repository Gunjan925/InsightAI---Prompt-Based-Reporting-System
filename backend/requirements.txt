fastapi==0.115.0 # to build REST APIs
uvicorn==0.30.1 # ASGI server used to run the FastAPI application
sqlalchemy==2.0.30 # ORM (Object Relational Mapper) used to interact with MySQL using Python objects
pymysql==1.1.1 # MySQL database driver used by SQLAlchemy to connect to MySQL
# Provides cryptographic functions required for secure database connections,
# password hashing and SSL/TLS encryption
cryptography==42.0.8
pydantic==2.9.2 # Validates API request and response data using Python models
pydantic-settings==2.5.2 # Loads application settings from .env using Pydantic
pydantic[email]==2.9.2 # for email validation
python-dotenv==1.0.1 # Reads environment variables from the .env file
python-multipart==0.0.9 # Required for handling multipart/form-data requests (file uploads)
pyjwt==2.8.0 # Creates and verifies JWT authentication tokens
passlib[bcrypt]==1.7.4 # Hashes and verifies user passwords using bcrypt
bcrypt==4.0.1 # Compatible version required by Passlib 1.7.4
httpx==0.27.2 # Sends HTTP requests to external APIs (used to communicate with the AI service)
numpy==1.26.4 
pandas==2.2.3